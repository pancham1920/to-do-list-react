import styled from 'styled-components'
import { base } from '../appstyle';

export const Layout = styled.div`
  .new-todo {
    ${base.textInput};
    padding: 16px 16px 16px 60px;
    border: none;
  }
`